apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
data:
  BACKEND_URL: "http://backend-service.dev.svc.cluster.local:5000"
  
  # OLD SETUP: Local PostgreSQL in Kubernetes cluster
  # DATABASE_URI: "postgresql://ecommerce:testing123@database-service.dev.svc.cluster.local:5432/ecommerce_db"
  
  # NEW SETUP: DATABASE_URI now comes from AWS Secrets Manager via External Secrets Operator
  # No DATABASE_URI in ConfigMap - handled by rds-secret from Secrets Manager
